#include <iostream>
#include <vector>

using namespace std;

int main(){
	vector<int> currentVector;
	currentVector.push_back (1);
	currentVector.push_back (2);
	currentVector.push_back (3);
	cout << currentVector[0] << endl;
}

// please make it so the code remove everything in the vector, then insert 4 to the vector ?
// or atleast what does this code do ? 
// no need to be specific if you have a clue where this goes just answer it anyway
